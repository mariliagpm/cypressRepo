import { EventEmitter } from 'events';
export declare class Groups {
    private events;
    private groups;
    constructor(events: EventEmitter);
    resetGroups(): void;
    handleGroupsEvents(): void;
    startGroupMaybe(msg: string): boolean;
    endGroupMayBe(msg?: string): boolean;
}
