import { CommandT } from '../common/command-names';
export declare const attachRequests: (dbg: string, allureAttachRequests: boolean, command: CommandT, opts: {
    compactAttachments: boolean;
}) => void;
